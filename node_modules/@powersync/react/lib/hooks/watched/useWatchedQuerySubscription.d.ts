import { WatchedQuery } from '@powersync/common';
/**
 * A hook to access and subscribe to the results of an existing {@link WatchedQuery} instance.
 * @example
 * export const ContentComponent = () => {
 * const { data: lists }  = useWatchedQuerySubscription(listsQuery);
 *
 * return <View>
 *   {lists.map((l) => (
 *     <Text key={l.id}>{JSON.stringify(l)}</Text>
 *   ))}
 * </View>;
 * }
 *
 */
export declare const useWatchedQuerySubscription: <ResultType = unknown, Query extends WatchedQuery<ResultType> = WatchedQuery<ResultType>>(query: Query) => Query["state"];
/**
 * @internal
 */
export declare const useNullableWatchedQuerySubscription: <ResultType = unknown, Query extends WatchedQuery<ResultType> = WatchedQuery<ResultType>>(query: Query | null) => Query["state"] | undefined;
