import { AbstractPowerSyncDatabase, CompilableQuery, WatchCompatibleQuery } from '@powersync/common';
import { AdditionalOptions } from './watch-types.js';
export type InternalHookOptions<DataType> = {
    query: WatchCompatibleQuery<DataType>;
    powerSync: AbstractPowerSyncDatabase;
    queryChanged: boolean;
    active: boolean;
};
interface WatchCompatibleQueryWithParams<T> extends WatchCompatibleQuery<T> {
    stringifiedParameters?: string;
}
export declare const checkQueryChanged: <T>(query: WatchCompatibleQueryWithParams<T>, options: AdditionalOptions) => boolean;
export declare const constructCompatibleQuery: <RowType>(query: string | CompilableQuery<RowType>, parameters: any[], options: AdditionalOptions) => {
    parsedQuery: WatchCompatibleQueryWithParams<RowType[]>;
    queryChanged: boolean;
};
export {};
